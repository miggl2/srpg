{
  "name": "srpg",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "vite build && node scripts/copy-to-docs.js"
  },
  "dependencies": {
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
